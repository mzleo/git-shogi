# AGENTS.md

このリポジトリをレビュー・変更する Codex エージェント向けの作業規約です。

## 基本方針

- コードレビューの報告は必ず日本語で書く。
- ユーザー影響、データ破壊、CI の回避可能性、予期しない例外を優先して確認する。
- 指摘は `Critical`、`High`、`Medium`、`Low` の重要度付きで、重要度順に記載する。
- 各指摘には対象ファイルと行番号、問題、再現条件、影響、具体的な修正案を含める。
- 実際のコード、テスト、CI、README、実行結果を根拠にし、推測だけで断定しない。
- レビュー依頼では、依頼されない限り実装変更、コミット、PR 作成を行わない。

## リポジトリの前提

このプロジェクトは将棋の棋譜・局面データを扱う。主な確認対象は次のとおり。

- `data/games.json`: 対局データの正本
- SFEN パーサー／局面遷移検証: 構文、合法手、手番、手数、成り、持ち駒、王手・詰み
- SVG レンダラー: 局面の盤面画像化と PR の Before/After プレビュー
- `scripts/`: データ検証、更新、SVG 生成、CI 用 CLI
- `tests/`: ルール、SFEN、JSON 更新、レンダリング、CI 連携
- GitHub Actions: PR の base/head 差分の検証とプレビュー
- 品質確認: 固定された `uv` 環境で Black、flake8、pytest を実行

実際のコードと設定がこの文書と異なる場合は、現在のコードを優先する。

## PR レビューの必須確認

### issue と変更範囲

- PR の説明にある全 issue／要件を一つずつ実装・テストと対応付ける。
- README の対応済み機能と実装が一致するか確認する。
- 新規・削除・ID 変更・ID 重複・配列順変更・メタデータ変更を確認する。
- コードだけでなく、CI、CLI、README、テスト環境、依存関係も対象にする。

### base/head 比較

- `HEAD^` を PR の比較元にしていないか確認する。複数コミットでは PR の base SHA と head を比較する。
- 検証と Before/After レンダリングの両方に、正しい base SHA が渡ることを確認する。
- 先行コミットで不正変更し、最終コミットでは別ファイルだけを変更するケースをテストする。
- 1コミットの合法／不正変更、2件目以降のゲーム変更、新規・削除・複数ゲーム変更を確認する。
- Actions のイベント、checkout の履歴深度、base SHA の取得、スクリプト引数を確認する。

### SFEN と将棋ルール

- パーサーとレンダラーの受理条件を一致させる。
- `+P`、`+L`、`+N`、`+S`、`+B`、`+R` 以外の成駒（`+G`、`+K` など）を拒否する。
- 二重成り、升数不一致、駒数不正、手番・手数・持ち駒不正、未知記号を確認する。
- 後手を含む全駒の合法手、遮蔽、成り、不成、強制成り、再成り禁止を確認する。
- 駒打ち、二歩、行き所のない駒打ち、打ち歩詰め、王手放置、王手回避、合駒、ピンを確認する。
- 成駒を捕獲したとき未成の持ち駒へ戻ることを確認する。

### SVG とデータ更新

- 変更された `gameId` を抽出し、先頭ゲームを無条件に表示していないか確認する。
- base 側を Before、head 側を After として、変更された各ゲームを表示する。
- SVG が検証済み入力で `KeyError` などを起こさないことを確認する。
- JSON の必須フィールドについて、存在だけでなく型、空文字、ID 一意性、値の整合性を検証する。
- CLI の引数排他制御、終了コード、エラー時のファイル未変更、原子的な更新を確認する。

## テスト方針

- 正常系だけでなく、境界値、拒否、例外、終了コード、失敗時のファイル未変更を確認する。
- 複数コミットで `HEAD^` では検出できない回帰テストを必須とする。
- 内部実装ではなく、利用者から見える結果を検証する。
- 標準チェックは次の順序で実行する。

```text
uv run black --check scripts tests
uv run flake8 scripts tests
uv run pytest -q
```

## レビュー結果の形式

最初に `Approve`、`Request changes`、またはコメントのみの結論を書く。続いて重要度順に指摘し、最後に以下をまとめる。

- issue／要件の対応状況
- テスト実行結果
- 未確認事項・環境上の制約
- 推奨判定

レビュー結果は日本語で、開発者がそのまま修正へ移れる具体性で記載する。
